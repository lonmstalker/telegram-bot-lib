package io.lonmstalker.core;

/**
 * Типы прокси, поддерживаемые библиотекой.
 * Порядок констант соответствует значению, сохраняемому в БД.
 */
public enum ProxyType {
    HTTP,
    SOCKS4,
    SOCKS5
}
